#!/usr/bin/env bash

install-go() {
	if [[ $OSTYPE =~ ^darwin ]]; then
		brew install neovim
		return
	fi
	if [[ $OSTYPE =~ ^msys ]]; then
		winget install --id neovim.neovim
		return
	fi
	sudo apt install -y neovim
}

install-neovim
